# This project is a Python messenger which uses Kivy for the UI and MQTT.
## This project is a Python messenger which uses Kivy for the UI and MQTT.
### This project is a Python messenger which uses Kivy for the UI and MQTT.
